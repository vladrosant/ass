# plans

one file per initiative. each file holds the plan (phases, concepts, resources) and a running progress log (dated entries, checkboxes). update the checkboxes and log as work actually happens, don't rewrite history.

| initiative | status | doc |
|---|---|---|
| close the sim loop | in progress | [sim-loop.md](sim-loop.md) |

future candidates (see main README's "planned, not yet implemented" list): a real cnn training pipeline, rnn trajectory prediction, an rl avoidance policy, fault-tolerant thruster handling. these get their own doc here when they're picked up.
